<div class="container">
  <div class="text-center">
    <ul id="tab-menu">
      <li><a href="#tab1" class="active">週間ランキング</a></li>
      <li><a href="#tab2" class="">月間ランキング</a></li>
    </ul>
    <div id="tab-contents">
          <div id="tab1" class="tab">
            <table class="ranking-table">
              <tr>
                <th class="rank">順位</th>
                <th class="rank-user-name"> ユーザ名</th>
                <th class="rank-hour">勉強時間</th>
              </tr>
              <% rank_count = 0 %>
              <% @weekly_results.each do |  user , min | %>
              <tr>
                <td><%= rank_count += 1 %>位</td>
                <td><% user = User.find(user) %>
                <div class="text-left">
                    <%= link_to user_path(user.id), method: :get do %>
                    <%= attachment_image_tag user, :profile_image, fallback:"no_image.jpg", class:"mt-3 rounded-circle border" %><%= user.name %>
                    <% end %>
                </div>
                </td>
                <td><%= (min/60.0).floor(2) %>時間</td>
              </tr>
              <% end %>
            </table>
            </div>
            <div id="tab2" class="tab">
              <p>TAB2のテキストが入ります。TAB2のテキストが入ります。</p>
                <table class="ranking-table">
              <tr>
                <th class="rank">順位</th>
                <th class="rank-user-name"> ユーザ名</th>
                <th class="rank-hour">勉強時間</th>
              </tr>
              <% rank_count = 0 %>
              <% @monthly_results.each do |  user , min | %>
              <tr>
                <td><%= rank_count += 1 %>位</td>
                <td><% user = User.find(user) %>
                <div class="text-left">
                    <%= link_to user_path(user.id), method: :get do %>
                    <%= attachment_image_tag user, :profile_image, fallback:"no_image.jpg", class:"mt-3 rounded-circle border" %><%= user.name %>
                    <% end %>
                </div>
                </td>
                <td><%= (min/60.0).floor(2) %>時間</td>
              </tr>
              <% end %>
            </table>
            </div>
          </div>
        </div>
      </div>
    </div>


